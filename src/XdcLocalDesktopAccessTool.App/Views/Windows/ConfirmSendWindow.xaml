<Window x:Class="XdcLocalDesktopAccessTool.App.Views.Windows.ConfirmSendWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm Send"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Border Padding="14" BorderBrush="#A0A0A0" BorderThickness="1" CornerRadius="8" Background="#F5F5F5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="16"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="Please confirm the transaction details:"
                       FontWeight="Bold"
                       FontSize="14"
                       Margin="0,0,0,2"/>

            <TextBox Grid.Row="2"
                     x:Name="DetailsTextBox"
                     IsReadOnly="True"
                     MinWidth="520"
                     MinHeight="160"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled"
                     FontFamily="Consolas"
                     FontSize="12"/>

            <StackPanel Grid.Row="4"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="Cancel" Width="110" Margin="0,0,8,0" Click="Cancel_Click"/>
                <Button Content="Confirm" Width="110" Click="Confirm_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
